#include <stdlib.h>
#include <stdio.h>

/**
* main - Entry point
*
* print the alphabet in lowercase *
*
* Return: Always 0 (Success)
*/
int main(void)

{
char x;
for(char x = 'a'; x <= 'z'; x++);
putchar(x);
 
putchar('\n');
return (0);
}
