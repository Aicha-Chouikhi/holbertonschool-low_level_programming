#include <stdio.h>
#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @x: first number
 * @y: second number
 *
 * Return: integer value
 */

int add(int x, int y)
{
return (x + y);
}
