#include "main.h"

/**
 * _puts - is a function that prints a string
 * @str: input
 *  Return: Always 0.
 */
void _puts(char *str)
{

int i = 0;
for (str [i] != "\0", i++)
{
printf (%c, str[i]);
}
printf ("\n");
}
